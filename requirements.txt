# Install torch CPU-only FIRST to avoid pulling a CUDA build via EasyOCR:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

yt-dlp>=2026.8.19
opencv-python-headless>=5.0.0
easyocr>=1.7.2
faster-whisper>=1.2.1
imageio-ffmpeg>=0.6.0
rapidfuzz>=3.14.5
typer>=0.27.1
rich>=15.0.0
questionary>=2.1.1
numpy>=2.0.0
pillow>=12.0.0

# dev / test
pytest>=8.0.0
